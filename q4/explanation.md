# Question 4 Explanation

The investigation used open file descriptors and process information to show how Linux manages file access internally. Standard output and standard error were redirected into separate and combined files to demonstrate how I/O streams are handled.
