# Question 3 Explanation

A hard link and the original file share the same inode, so the file content remains accessible even after the original name is removed. A symbolic link points to a path, so it becomes broken if the target file is deleted.
